# UI Journey MCP

Query mapped UI flows, versioned browser evidence and before/after screenshots
from a local MCP client or the command line. Independent of any application,
GitHub account or other MCP server.

The initial implementation is being prepared in a pull request. Evidence belongs
in a separate private data directory; publishing this tool does not publish your
application's screenshots or journey data.

Licensed under MIT.
